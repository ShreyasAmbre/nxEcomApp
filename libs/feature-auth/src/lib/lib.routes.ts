import { Route } from '@angular/router';
import { FeatureAuthComponent } from './feature-auth/feature-auth.component';

export const featureAuthRoutes: Route[] = [
  { path: '', component: FeatureAuthComponent },
];
